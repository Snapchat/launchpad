# Steps to publish new template:
```bash
aws s3 cp template-mpc.yml s3://snap-launchpad-public/cloudformation/template-mpc.yml
aws s3 cp template-relay.yml s3://snap-launchpad-public/cloudformation/template-relay.yml 
```
